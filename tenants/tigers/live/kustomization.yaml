apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: live-tigers-
commonLabels:
  toolkit.fluxcd.io/tenant: tigers
  environment: live

resources:
  - ../rbac.yaml
  - ../sync.yaml
